/**
 * Catálogo de permissões para a tela de configuração por usuário (Equipe > Permissões).
 * Re-exporta PERMISSIONS do mapa para manter compatibilidade.
 */
export { PERMISSIONS } from "./permissions.map.js";
