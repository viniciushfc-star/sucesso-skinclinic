Conteúdo seguro para colar nos arquivos durante o rebase (quando o Git parar em "edit"):

--- js/ui/google.txt (substitua o conteúdo inteiro) ---
SEU_GOOGLE_CLIENT_ID.apps.googleusercontent.com

chave secreta do cliente (nunca commitar a real)

SEU_GOOGLE_CLIENT_SECRET

--- .env.example: linha 4 (troque só a linha do OPENAI_KEY) ---
OPENAI_KEY=sk-your-openai-key-here
